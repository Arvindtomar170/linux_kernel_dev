# Create initramfs from the current kernel
    1. sudo mkinitramfs -o output ( for Ubuntu/Debian)
# lightweight initramfs file system
    (using buzy-box)
        refer -> crfs.sh script
# linux_kernel_dev
